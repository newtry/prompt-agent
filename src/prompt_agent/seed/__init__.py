"""Built-in seed prompts shipped with PromptAgent.

Each subdirectory contains a `v1.md` (the prompt) and optionally a `suite.yaml`
(test cases for `pa eval`). These are loaded via `importlib.resources`.
"""